import ChatContainer from '@/react-app/components/ChatContainer';

export default function Home() {
  return (
    <div className="h-screen flex flex-col bg-gray-50">
      <ChatContainer />
    </div>
  );
}
